{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "remote-db.schema.json",
	"title": "Remote Db",
	"description": "Configuration to connect and replicate to a remote CouchDB",
	"type": "object",
	"properties": {
		"url": {
			"description": "The remote db url",
			"type": "string"
		},
		"direction": {
			"description": "Direction of replication between source and remote",
			"type": "string",
			"enum": ["to", "from", "sync"]
		},
		"live": {
			"description": "Flag indicating if the replicators connection should be live, watch and sync new changes as they become available",
			"type": "boolean"
		},
		"retry": {
			"description": "Flag indicating if the replicators connection should automatically retry on failure",
			"type": "boolean"
		}
	},
	"required": ["url"]
}
