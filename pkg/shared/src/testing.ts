/** @TODO Set up exports so that we can use this as '@librocco/shared/testing' */

export type WebClientView = "search" | "inventory" | "outbound" | "settings" | "inbound-note" | "outbound-note";
export type WebClientInventoryView = "warehouses" | "inbound";
